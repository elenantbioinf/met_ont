# Environments

This directory contains environment definitions used in the analysis (conda environments).

Each environment specifies the software and dependencies required for a specific step.

This ensures reproducibility across different systems.