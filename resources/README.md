# Resources

This directory contains static resources required by the pipeline, such as the reference genome and external model files.

These files are used as inputs by different modules and are not generated by the pipeline itself.